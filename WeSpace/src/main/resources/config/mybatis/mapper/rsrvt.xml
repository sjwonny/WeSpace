<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rsrvt">
<select id="selectrsrvtCnt" resultType="int" parameterType="int" > 
select count(rsrvt_no)as rsrvtCnt from rsrvt where RSRVT_amnt >= 10000 and guest_no = #{guest_no} 
</select>



</mapper>
	